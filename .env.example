# Kopier til .env.local for lokal utvikling. Aldri commit ekte verdier —
# se INFRASTRUCTURE.md 9.

# Postgres (Neon i Stadium 0, se INFRASTRUCTURE.md 16 — Aiven i Stadium 1)
DATABASE_URL=postgres://user:password@localhost:5432/kildebanken

# Maks antall samtidige tilkoblinger i poolen (src/db/client.ts). Valgfri —
# faller tilbake til 3 (Stadium 0, se INFRASTRUCTURE.md 4/16.8) hvis utelatt.
# Kan økes i Stadium 1 (alltid-kjørende prosess, ikke serverløse funksjoner).
DB_POOL_MAX=

# Brevo (SPEC-V1.md / INFRASTRUCTURE.md 6.2 — besluttet)
BREVO_API_KEY=
BREVO_SENDER_TRANSACTIONAL=varsler@tjenesten.no
BREVO_SENDER_BULK=utsendelse@epost.tjenesten.no

# Delt hemmelighet for POST /webhooks/email-events (SPEC-V1.md 10.1, 10.3,
# 20 — se src/lib/subscriptions/email-events.ts). Satt som en query-
# parameter i webhook-URL-en registrert hos Brevo. UTEN denne satt avvises
# ALLE webhook-kall (fail closed, samme prinsipp som RETENTION_DRY_RUN under).
EMAIL_WEBHOOK_SECRET=

# Sentry (INFRASTRUCTURE.md 3, EU-region) — vedtatt leverandør, selve
# integrasjonen er ikke bygget ennå (se NATTLOGG.md). Uten en reell verdi her
# gjør koden ingenting Sentry-spesifikt; ufarlig å la stå tom.
SENTRY_DSN=

# Brukes i digest-e-postens lenker (src/lib/email/digest.ts). Sett til det
# faktiske domenet i produksjon/test.
NEXT_PUBLIC_SITE_ORIGIN=http://localhost:3000

# Retensjonsjobben (SPEC-V1.md 17.4, src/lib/jobs/retention.ts) kjører som
# "dry run" med mindre denne er EKSPLISITT satt til "false". Sett den ALDRI
# til "false" mot et miljø med ekte personopplysninger uten at noen faktisk
# har verifisert dry run-loggene først.
RETENTION_DRY_RUN=true
